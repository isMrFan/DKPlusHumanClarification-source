{
  "humanClarification.templates.contact": [
    {
      "name": "implement",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n你现在是编码执行代理。默认直接解决任务，不要停在泛泛分析或只给计划。先从最具体的文件、符号、报错或行为入口开始，形成一个可证伪的局部假设，再做最小修改。若需求本身不清楚，请使用 `request_user_clarification`；若执行被阻塞或需要我决策，请使用 `request_contact_user`；结束前请使用 `request_user_feedback`。"
    },
    {
      "name": "debug",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请按调试任务处理：先定位复现入口和控制路径，再给出一个可证伪的根因假设，随后做最小修复。不要用大范围重构掩盖问题。缺少信息时用 `request_user_clarification`，遇到阻塞时用 `request_contact_user`，结束前用 `request_user_feedback`。"
    },
    {
      "name": "review",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请按代码评审模式处理，优先找 bug、行为回归、边界条件风险和缺失校验，不要把总结放在前面。输出要简洁，问题按严重程度排序。若上下文不足，请用 `request_user_clarification`；结束前用 `request_user_feedback`。"
    },
    {
      "name": "continue",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n继续当前任务并直接向前推进，不要重复既有计划，不要提前结束。若需要我确认方向或取舍，请使用 `request_contact_user`；完成后使用 `request_user_feedback`。"
    },
    {
      "name": "plan",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请只做规划，不要实现。先用只读方式收集足够上下文，再输出一个短而清晰、可执行的计划草案供我确认。若任务边界不清楚，请先使用 `request_user_clarification`；计划给出后请使用 `request_user_feedback` 收集我的意见。"
    }
  ],
  "humanClarification.templates.feedback": [
    {
      "name": "continue",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n根据这条反馈继续推进，不要回到泛泛分析。优先处理最关键的问题，必要时再缩小到最小修复。若需要额外信息，请使用 `request_user_clarification`；结束前继续用 `request_user_feedback` 与我沟通。"
    },
    {
      "name": "clarify",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n这条反馈说明任务边界还不够清楚。请先通过 `request_user_clarification` 继续追问关键信息，再决定后续动作，不要擅自结束对话。"
    },
    {
      "name": "review-fix",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请把这条反馈当作 review comment 处理：先确认它指向的真实风险，再做最小修复，不要顺手扩散到无关区域。若评论不够具体，请用 `request_user_clarification`；完成后用 `request_user_feedback` 继续确认。"
    },
    {
      "name": "summarize-next",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请先总结当前已完成内容、剩余风险和下一步建议，再通过 `request_user_feedback` 继续和我确认后续动作。除非我明确要求，不要直接结束。"
    }
  ],
  "humanClarification.templates.clarification": [
    {
      "name": "clarify",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请通过 `request_user_clarification` 继续补齐关键需求信息。优先确认这几类内容：业务目标、当前行为、期望行为、输入输出样例、边界条件、验收标准。每轮只提出最能推进判断的 1 到 3 个问题，避免泛泛追问或一次抛出过多问题。"
    },
    {
      "name": "constraints",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n在继续之前，请先通过 `request_user_clarification` 确认实施约束和决策边界：是否允许修改接口或数据结构、是否允许引入新依赖、是否必须兼容既有行为、运行环境与版本要求、性能或安全约束、时间范围与改动边界。"
    },
    {
      "name": "repro",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n请先通过 `request_user_clarification` 获取最小可复现闭环，再继续处理。至少确认：前置条件、触发步骤、输入数据或样例、当前结果、期望结果、相关命令、日志或报错信息，以及问题是否稳定复现。"
    },
    {
      "name": "handoff-to-feedback",
      "template": "{{INPUT_CONTENT}}\n\n---\n\n如果核心需求和事实信息已经足够清楚，剩下的只是方案偏好、优先级取舍或验收倾向，请改用 `request_user_feedback` 与用户继续沟通，不要长期停留在信息澄清阶段。"
    }
  ]
}